<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Blog Post from <%= name %></title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        line-height: 1.6;
        color: #333;
        background-color: #f5f5f5;
      }

      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: white;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      }

      .header {
        background: linear-gradient(135deg, #6b5b45 0%, #c8956e 100%);
        color: white;
        padding: 40px 20px;
        text-align: center;
      }

      .header h1 {
        font-size: 28px;
        margin-bottom: 10px;
      }

      .header p {
        font-size: 14px;
        opacity: 0.9;
      }

      .content {
        padding: 40px 20px;
      }

      .blog-card {
        margin: 30px 0;
        border: 1px solid #e0e0e0;
        border-radius: 8px;
        overflow: hidden;
      }

      .blog-image {
        width: 100%;
        height: 280px;
        object-fit: cover;
        display: block;
      }

      .blog-details {
        padding: 25px;
      }

      .blog-category {
        display: inline-block;
        background: #f0f0f0;
        color: #8b6f47;
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 12px;
        font-weight: 600;
        margin-bottom: 10px;
      }

      .blog-title {
        font-size: 24px;
        color: #2c3e50;
        margin-bottom: 12px;
        line-height: 1.3;
      }

      .blog-meta {
        display: flex;
        align-items: center;
        margin-bottom: 15px;
        padding-bottom: 15px;
        border-bottom: 1px solid #f0f0f0;
      }

      .blog-avatar {
        width: 44px;
        height: 44px;
        border-radius: 50%;
        margin-right: 12px;
        object-fit: cover;
      }

      .blog-meta-info h3 {
        margin: 0;
        font-size: 15px;
        color: #2c3e50;
      }

      .blog-meta-info p {
        margin: 4px 0 0 0;
        font-size: 12px;
        color: #7f8c8d;
      }

      .blog-excerpt {
        color: #555;
        font-size: 14px;
        line-height: 1.7;
        margin: 15px 0;
      }

      .blog-excerpt strong {
        color: #2c3e50;
        font-weight: 600;
      }

      .cta-button {
        display: inline-block;
        background: linear-gradient(135deg, #8b6f47 0%, #d4a574 100%);
        color: white;
        padding: 12px 30px;
        text-decoration: none;
        border-radius: 5px;
        margin-top: 20px;
        font-weight: 600;
        transition: opacity 0.3s;
      }

      .cta-button:hover {
        opacity: 0.9;
      }

      .divider {
        height: 1px;
        background: #e0e0e0;
        margin: 30px 0;
      }

      .footer {
        background-color: #f9f9f9;
        padding: 20px;
        text-align: center;
        font-size: 12px;
        color: #999;
        border-top: 1px solid #e0e0e0;
      }

      .footer p {
        margin: 8px 0;
      }

      .unsubscribe {
        color: #8b6f47;
        text-decoration: none;
      }

      @media (max-width: 600px) {
        .container {
          width: 100% !important;
        }

        .header {
          padding: 30px 15px;
        }

        .header h1 {
          font-size: 22px;
        }

        .content {
          padding: 20px 15px;
        }

        .blog-image {
          height: 220px;
        }

        .blog-title {
          font-size: 20px;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <!-- Header -->
      <div class="header">
        <h1>📝 New Blog Post!</h1>
        <p>A fresh culinary story from our community</p>
      </div>

      <!-- Content -->
      <div class="content">
        <p style="margin-bottom: 20px; color: #555">Hello there,</p>

        <!-- Blog Card -->
        <div class="blog-card">
          <img
            src="<%= blog.featuredImage %>"
            alt="<%= blog.title %>"
            class="blog-image"
          />

          <div class="blog-details">
            <span class="blog-category"><%= blog.category %></span>

            <h2 class="blog-title"><%= blog.title %></h2>

            <!-- Blog Meta -->
            <div class="blog-meta">
              <img
                src="<%= blog.author.avatar %>"
                alt="<%= name %>"
                class="blog-avatar"
              />
              <div class="blog-meta-info">
                <h3><%= name %></h3>
                <p><%= blog.createdAt %></p>
              </div>
            </div>

            <!-- Blog Excerpt -->
            <p class="blog-excerpt"><%= blog.excerpt %></p>

            <a href="<%= blogUrl %>" class="cta-button">Read Full Article</a>
          </div>
        </div>

        <div class="divider"></div>

        <p style="color: #7f8c8d; font-size: 14px">
          Stay tuned for more culinary insights and cooking stories from our
          talented community of chefs.
        </p>
      </div>

      <!-- Footer -->
      <div class="footer">
        <p>
          You're receiving this email because you subscribed to blog updates.
        </p>
        <p>&copy; 2026 Recipe Blogger. All rights reserved.</p>
      </div>
    </div>
  </body>
</html>
